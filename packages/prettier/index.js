/*
 * @Author: wangzhihao
 * @Date: 2021-11-17 10:02:53
 * @LastEditors: wangzhihao
 * @LastEditTime: 2021-11-30 12:48:10
 */
module.exports = {
    tabWidth: 4,
    semi: true,
    singleQuote: true,
    jsxSingleQuote: true,
    bracketSpacing: true,
    printWidth: 120,
    arrowParens: 'avoid',
}
// printWidth 编辑器每行的长度，默认80
// tabWidth 制表符tab的宽度，默认值是2
// useTabs 代码缩进是否用制表符tab，默认false
// semi 是否使用分号，默认true，使用分号
// singleQuote 是否使用单引号，默认为false，不适用单引号，使用双引号

// quoteProps 对象属性的引号使用
// as-needed 仅在需要的时候使用
// consistent 有一个属性需要引号，就都需要引号
// preserve 保留用户输入的情况

// trailingComma 末尾逗号
// none 末尾没有逗号
// es5 es5有效的地方保留
// all 在可能的地方都加上逗号

// bracketSpacing 字面量对象括号中的空格，默认true
// true - Example: { foo: bar }.
// false - Example: {foo: bar}.

// arrowParens 箭头函数中的括号
// “avoid” - 在有需要的时候使用. Example: x => x
// “always” - 一直使用. Example: (x) => x
